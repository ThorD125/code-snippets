file explorer
function on click of item {
    if (item is file){
        preview file
    }
    else  if (item is folder){
        return folder structure
    } else if (item is link{

    } 
    esle {
        error in file
    }
}

functions
